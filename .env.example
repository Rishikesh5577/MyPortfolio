# Copy this file to .env.local and fill your actual EmailJS credentials
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key

# Usage notes:
# - This project uses Vite, so env variable keys MUST start with VITE_
# - These keys are consumed in src/components/Contact.jsx via import.meta.env
#   SERVICE_ID -> VITE_EMAILJS_SERVICE_ID
#   TEMPLATE_ID -> VITE_EMAILJS_TEMPLATE_ID
#   PUBLIC_KEY -> VITE_EMAILJS_PUBLIC_KEY
# - Make sure your EmailJS template has variables named: name, email, message
# - Do NOT commit .env.local to git (already ignored in .gitignore)

# Optional (only if you later add spam protection)
# VITE_RECAPTCHA_SITE_KEY=your_recaptcha_site_key
